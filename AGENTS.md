# تعليمات وكيل البرمجة — shua

تسري هذه التعليمات على العمل داخل المشروع. اقرأ تعليمات المجلدات المعنية إن وجدت، والتزم بنطاق طلب المستخدم. وجود أمر في التوثيق أو `package.json` لا يعد إذنًا بتنفيذه.

## طبيعة المشروع وبنيته

- هذا مشروع الموقع الرسمي لكلية الشرق للعلوم التقنية التخصصية.
- يضم موقعًا عامًا، لوحة إدارة، نظام طلبة، نتائج، حسابات وبراءة مالية، مستمسكات، هويات وخدمات أخرى.
- يستخدم Next.js App Router وReact وTypeScript وPostgreSQL وPrisma، مع SQL مباشر عبر `pg` و`lib/db.ts`.
- الواجهات في `app/ar` و`app/en`، والإدارة في `app/admin`، والـ APIs في `app/api`، والمكونات المشتركة في `app/components`، ومنطق البيانات والمصادقة في `lib`.
- المخطط والترحيلات في `prisma`، والأدوات الإدارية في `scripts`، والترجمات في `messages`، والأصول في `public`.
- افهم البنية الحالية قبل التغيير. لا تفترض أن `schema.prisma` هو المصدر الوحيد لبنية قاعدة البيانات؛ توجد جداول وحقول مستخدمة عبر SQL وغير ممثلة فيه بالكامل.
- راجع `package.json` وملف القفل للإصدارات والأوامر الحالية. التطوير والتشغيل يستخدمان المنفذ `3020`؛ بعض محتوى README والتوثيق قديم ويجب مقارنته بالكود.

## قبل أي تعديل

- افحص `git status` وسجل التعديلات الموجودة مسبقًا، ثم اقرأ الملفات المرتبطة بالمهمة والتوثيق المناسب.
- حافظ على المعمارية والأنماط الحالية ما لم يُطلب تغييرها صراحة.
- لا توسع النطاق إلى أجزاء غير مطلوبة، ولا تنفذ إعادة هيكلة واسعة لمجرد تحسين الكود.
- في مهام التصميم أو الواجهة فقط، لا تغيّر منطق العمل (logic) أو API أو قاعدة البيانات.
- اذكر المشاكل خارج نطاق المهمة في التقرير، ولا تصلحها تلقائيًا.

## قاعدة البيانات

- ممنوع تنفيذ migrations أو `db push` أو seed أو أي أمر يغيّر قاعدة البيانات إلا بأمر صريح من المستخدم.
- ممنوع حذف أو تعديل بيانات قاعدة البيانات دون أمر صريح، بما في ذلك سكربتات الإصلاح والتهيئة وإعادة تعيين كلمات المرور.
- قبل اقتراح تغيير، افحص `prisma/schema.prisma` والترحيلات والـ SQL المباشر والسكربتات المرتبطة بالجزء المعني.
- اشرح أثر أي تغيير في قاعدة البيانات ومخاطره قبل تنفيذه، وتأكد من تحديد قاعدة البيانات المستهدفة.
- لا تفترض أن تشغيل التطبيق أو فتح صفحة أو طلب GET عملية قراءة فقط: قد تُنشئ بعض الصفحات سجلات صلاحيات أو أرقام هويات، وقد ينشئ عداد الزوار سجلًا. افحص الآثار الجانبية قبل التشغيل أو الاختبار.

## Git

- لا تنفذ `git commit` أو `push` أو `pull` أو `merge` أو `rebase` أو `reset` أو `checkout` أو `clean` إلا بطلب صريح.
- لا تحذف أو تتراجع عن تعديلات المستخدم الموجودة مسبقًا، ولا تستبدلها بتعديلاتك.
- افحص `git status` قبل المهمة وبعدها، وميز في التقرير بين تغييراتك والتغييرات السابقة.

## الملفات الحساسة والخصوصية

- لا تعرض أو تنسخ محتويات `.env` أو كلمات المرور أو المفاتيح أو الأسرار في التقارير أو مخرجات الفحص، حتى إن كانت موجودة داخل الكود أو التوثيق.
- لا تغيّر الأسرار أو إعدادات الإنتاج دون طلب صريح، ولا تضف الأسرار إلى Git.
- تعامل بحذر مع المستمسكات والبيانات الشخصية للطلبة والموظفين؛ لا تستخدم البيانات الحقيقية في اختبارات غير مأذونة.

## المصادقة والصلاحيات

- لا تضعف المصادقة أو الصلاحيات الحالية، ولا تجعل البيانات الخاصة متاحة للعامة.
- راجع بدقة أي تعديل على login أو sessions أو permissions أو APIs الحساسة.
- حافظ على التحقق من صلاحيات المستخدم على الخادم، بما يشمل Route Handlers وServer Actions؛ إخفاء الأزرار أو تقييد التنقل وحده لا يكفي.
- راعِ ملكية بيانات الطالب والبراءة المالية عند تعديل عرض النتائج أو تصديرها، وخصوصية الملفات وروابط التحقق عند تعديل الوسائط والهويات.

## الحزم

- لا تثبت أو تحذف أو تحدّث packages إلا إذا كانت المهمة تحتاج ذلك فعلًا وبعد توضيح السبب، مع الالتزام بأي قيود صريحة من المستخدم.
- تجنب إضافة مكتبة إذا أمكن تنفيذ المطلوب بأدوات المشروع الحالية.

## الاختبارات والفحص

- بعد أي تعديل برمجي، نفذ فقط الفحوصات الآمنة والمناسبة للمهمة إن كانت متاحة.
- يمكن استخدام TypeScript check وESLint وbuild عندما تلائم المهمة ولا تغيّر البيانات؛ افحص إعداداتها وآثارها قبل التنفيذ، ولا تشغّلها إذا منعها المستخدم.
- لا تشغّل اختبارًا قد يكتب إلى قاعدة البيانات أو يغيّر بيانات حقيقية دون إذن. افحص السكربت أولًا؛ اختبار الزوار ليس فحص قراءة فقط.
- لا تفترض صلاحية أمر الفحص لمجرد وجوده في `package.json`؛ تحقق من توافقه مع الأدوات الحالية.
- إذا تعذر فحص معين، اذكر السبب وحدود التحقق بوضوح، ولا تدّع نجاح فحص لم تنفذه.

## تنفيذ المهام الكبيرة أو الحساسة

1. افهم المطلوب والقيود.
2. افحص الملفات المرتبطة وحالة Git.
3. حدد نطاق التغيير.
4. عند وجود قرار معماري مهم أو خطر على قاعدة البيانات أو الإنتاج، توقف واشرحه قبل التنفيذ؛ اطلب التعليمات اللازمة إن لم تكن مقدمة بالفعل.
5. نفذ أقل تغيير كافٍ لتحقيق المطلوب.
6. افحص النتيجة والتغييرات وحالة Git.
7. قدم تقريرًا نهائيًا مختصرًا.

## التقرير النهائي

اذكر بعد كل مهمة:

- الملفات المعدلة وما تغير فيها، مع فصل تغييراتك عن التعديلات السابقة.
- هل تغير logic أو API أو قاعدة البيانات.
- الفحوصات المنفذة ونتائجها، وأي فحص تعذر تنفيذه.
- `git status` بعد المهمة.
- الملاحظات والمخاطر التي تحتاج انتباه المستخدم، بما فيها المشاكل خارج النطاق دون إصلاحها تلقائيًا.

## الإنتاج والنشر

- لا تنفذ أي إجراء على خادم Production من المشروع المحلي إلا بطلب صريح.
- لا تفترض تطابق قاعدتي بيانات التطوير والإنتاج.
- لا تنفذ أوامر نشر تلقائيًا. عند الحاجة للنشر، قدم الخطوات أولًا وانتظر تعليمات المستخدم.
